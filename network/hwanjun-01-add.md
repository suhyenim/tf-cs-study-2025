# HTTP Method란?

HTTP Method는 **클라이언트가 서버에 어떤 동작을 수행하고 싶은지 표현하는 명령어**

RESTful API 설계의 핵심 요소이며, 의미론적(semantic) 기반으로 정의

### ✔ 주요 HTTP Method

| Method      | 목적                    | 특징                         |
| ----------- | ----------------------- | ---------------------------- |
| **GET**     | 리소스 조회             | 안전하며 멱등                |
| **POST**    | 리소스 생성/데이터 처리 | 멱등 아님                    |
| **PUT**     | 리소스 전체 업데이트    | 멱등                         |
| **PATCH**   | 리소스 부분 업데이트    | 구현에 따라 멱등 여부 달라짐 |
| **DELETE**  | 리소스 삭제             | 멱등                         |
| **HEAD**    | 헤더만 조회             | 안전 / 멱등                  |
| **OPTIONS** | 지원하는 Method 확인    | CORS 사전 요청에 사용        |

> 의미 기반으로 설계하는 것이 REST의 핵심이며, 잘못 사용할 경우 캐싱, 로드밸런서, 브라우저 동작 등이 예측과 다르게 작동할 수 있다

---

# 2️⃣ HTTP Method의 멱등성(Idempotency)

**멱등성**: 같은 요청을 여러 번 보내도 결과가 동일한 성질

### ✔ 멱등한 Method

- **GET, PUT, DELETE, HEAD**
- 네트워크 오류로 인한 자동 재시도에 안전함

### ✔ 멱등하지 않은 Method

- **POST, PATCH(일반적으로)**
- 중복 요청 → 중복 데이터 생성 가능
- 서버에서 **Idempotency Key 패턴**으로 멱등성 부여 가능 (결제 API 등)

### ✔ 안전성(Safety)

- 서버 상태를 변경하지 않는 성질
- **GET, HEAD만 안전함**

---

# 3️⃣ GET vs POST

### ✔ GET

- 조회(Read) 목적
- URL에 쿼리 포함
- 캐싱 가능
- 북마크/재요청 가능
- 안전 / 멱등

### ✔ POST

- 생성/처리(Create) 목적
- Body에 데이터 포함
- 캐싱 안 됨
- 멱등하지 않음
- 보안성이 더 높음(Body 노출 X)

> 복잡한 검색 쿼리의 경우 실용적으로 POST + Body(JSON) 를 사용하기도 함
>
> (Elasticsearch, GraphQL 등)

---

# 4️⃣ POST vs PUT vs PATCH

| Method    | 목적                      | 멱등성 | 특징                             |
| --------- | ------------------------- | ------ | -------------------------------- |
| **POST**  | 생성(Create)              | ❌     | 서버가 ID 생성                   |
| **PUT**   | 전체 교체(Update/Replace) | ⭕     | 리소스 없으면 생성(Upsert)       |
| **PATCH** | 부분 수정(Partial Update) | △      | JSON Patch 등으로 멱등 구현 가능 |

---

# 5️⃣ GET Body는 왜 지양하는가?

HTTP 1.1 스펙상 Body를 넣을 수는 있지만, **대부분의 서버·프록시·브라우저가 지원하지 않음**

- GET Body는 의미 없음으로 규정됨
- 캐싱 키 충돌 가능
- 인프라가 Body를 무시하는 경우 많음 → 예측 불가능
- REST 의미론적 혼란

**그래서 GET Body는 실무에서 사용하지 않는다**

---

# 6️⃣ HTTPS 보안 구조

HTTPS는 **대칭키 + 공개키 암호화**를 조합한 하이브리드 구조로 동작

### ✔ 대칭키 암호화 (AES 등)

- 같은 키로 암호화/복호화
- 속도 빠름 → 실제 데이터 전송에 사용

### ✔ 공개키 암호화 (RSA/ECC)

- 공개키로 암호화, 개인키로 복호화
- 속도 느림 → 대칭키 교환에만 사용

---

# 🔐 **HTTPS Handshake 과정 (TLS 1.2 & TLS 1.3)**

HTTPS는 “HTTP + TLS 암호화”이며, **핸드셰이크 과정에서 안전한 대칭키를 교환**한 뒤,

해당 대칭키(AES 등)로 빠르게 데이터 전송을 진행합니다.

---

# TLS 1.2 Handshake 흐름 (전통적인 HTTPS)

TLS 1.2는 **2-RTT (두 번 왕복)** 으로 작동

![image.png](attachment:6d00cdf3-d04e-4e5e-8eb3-1cd5566c070d:image.png)

## ✔ 전체 흐름 요약

```
ClientHello  →
            ←  ServerHello + Certificate + ServerKeyExchange
ClientKeyExchange + Finished  →
            ←  Finished
```

## ✔ 상세 단계

---

### **① ClientHello**

클라이언트가 서버에 TLS 연결을 요청하며 보내는 내용:

- 지원 가능한 TLS 버전
- 지원하는 Cipher Suite 목록 (암호 알고리즘 후보들)
- 랜덤 값(Client Random)
- 압축 방식 등 부가 정보

➡ 서버에게 “이 암호 알고리즘들 중 하나로 통신하자”고 제안하는 단계

---

### **② ServerHello + Certificate + ServerKeyExchange**

서버가 응답하며 보내는 내용:

1. **ServerHello**
   - 선택한 TLS 버전
   - 선택된 Cipher Suite
   - Server Random
2. **Certificate (인증서)**
   - 서버의 공개키 포함
   - CA가 개인키로 서명 → 클라이언트가 검증 가능
   - MITM 공격 방지의 핵심
3. **ServerKeyExchange**
   - Diffie-Hellman(DHE/ECDHE) 매개변수 전달
   - 공유 대칭키를 안전하게 생성하기 위한 값

---

### **③ Certificate 검증 (클라이언트 측)**

클라이언트는 서버 인증서를 다음 순서로 검증:

- 서명한 CA가 신뢰 가능한 CA 목록에 있는지
- 인증서 만료 여부
- 도메인 일치 여부
- 체인(Chain of Trust) 유효한지

➡ 인증서가 유효해야만 서버의 공개키를 신뢰할 수 있음

---

### **④ ClientKeyExchange**

클라이언트가 Diffie-Hellman 키 교환값을 서버에 전달

- 클라이언트와 서버는 **서로 랜덤값 + DH 결과를 조합해 “대칭키(Session Key)” 생성**
- 이제 동일한 대칭키를 공유하게 됨 → 이후 데이터 암호화 가능

---

### **⑤ Finished 메시지 교환**

- 클라이언트 → Finished (대칭키로 암호화된 첫 메시지)
- 서버 → Finished (클라이언트가 보낸 Finished 검증 후 응답)

➡ 이제 **암호화된 HTTPS 통신 시작**

---

### 🔚 TLS 1.2 정리

| 단계        | 내용                            |
| ----------- | ------------------------------- |
| ClientHello | 암호 알고리즘 제안              |
| ServerHello | 암호 알고리즘 선택              |
| Certificate | 서버 공개키 전달                |
| KeyExchange | 클라이언트와 서버가 대칭키 생성 |
| Finished    | 비밀 통신 준비 완료             |

**단점: 왕복 2회(2-RTT) → 느림**

---

# 2️⃣ TLS 1.3 Handshake (최신, 더 빠름)

TLS 1.3에서는 속도와 보안이 크게 개선됨.

### ✔ TLS 1.2: 2-RTT → TLS 1.3: 1-RTT (심지어 0-RTT도 지원)

---

# 🔥 TLS 1.3 Handshake 흐름

```
ClientHello →
            ← ServerHello + Finished
Client Finished →
```

TLS 1.3에서는:

- 불필요한 메시지 제거
- ServerKeyExchange 제거
- Cipher Suite 간소화
- Perfect Forward Secrecy(ECDHE) 의무화

## ✔ 단계 설명

---

### **① ClientHello**

TLS 1.2와 유사하지만

**KeyShare(ECDHE 키 교환 값)** 을 함께 전송함.

➡ 서버는 따로 ServerKeyExchange를 보낼 필요가 없어짐.

---

### **② ServerHello + EncryptedExtensions + Finished**

서버는:

- 선택한 암호 스위트
- 자신도 KeyShare(ECDHE 값) 전송
- Certificate (TLS1.2와 동일)
- CertificateVerify
- Finished 메시지

즉, 필요한 모든 정보를 한 번에 보냄.

---

### **③ 클라이언트 Finished**

클라이언트는 서버의 메시지를 검증 후:

- 자신도 Finished 메시지를 보냄
- 바로 암호화된 통신 시작

➡ 왕복 1번(1-RTT)으로 끝

---

# 3️⃣ TLS 1.3의 0-RTT (초고속 모드)

이전에 한 번 접속한 서버라면:

- handshake 없이 즉시 암호화된 데이터 전송 가능

하지만 재전송 공격(replay attack) 위험이 있어,

상태 변화 있는 요청(POST 등)에는 사용 금지됨.

---

# 4️⃣ TLS 1.2 vs TLS 1.3 비교

| 항목       | TLS 1.2                  | TLS 1.3                  |
| ---------- | ------------------------ | ------------------------ |
| 왕복 횟수  | 2-RTT                    | **1-RTT(빠름)**          |
| 0-RTT 모드 | ❌ 없음                  | **⭕ 지원**              |
| 보안성     | 복잡, 취약 알고리즘 존재 | **취약한 알고리즘 제거** |
| 키 교환    | 선택적 PFS               | **PFS(ECDHE) 강제**      |
| 속도       | 느림                     | **빠름**                 |
| 구성       | 많고 복잡                | **단순화**               |

---

# 5️⃣ 한눈에 보는 HTTPS Handshake 전체 그림

```
                [TLS 1.2]
Client                          Server
  | ------ ClientHello ---------> |
  | <--- ServerHello + Cert ----- |
  | ------ ClientKeyExchange ----> |
  | ------ Finished -------------> |
  | <------ Finished ------------- |

                [TLS 1.3]
Client                          Server
  | ------ ClientHello(KeyShare) -> |
  | <--- ServerHello + Finished ----|
  | ------ Finished ---------------> |

```

# 7️⃣ HTTPS Handshake에서 인증서를 사용하는 이유

### ❗ “이 공개키가 진짜 서버의 공개키인지 어떻게 증명할 것인가?”

이를 해결하는 것이 **인증서(Certificate)**.

### ✔ 인증서가 하는 일

1. 서버의 공개키 포함
2. 신뢰할 수 있는 인증기관(CA)이 서명
3. 클라이언트는 루트 인증서로 CA 서명을 검증
4. 서버의 공개키가 위조되지 않았음을 보장 → MITM 방지

> 즉, 공개키를 신뢰하기 위한 신원 검증 시스템이 바로 인증서 체계.

---

# 8️⃣ SSL vs TLS

SSL은 과거 프로토콜, 현재는 보안 취약점으로 사용 금지.

**TLS가 SSL의 후속 표준이며 실제 웹에서 사용되는 방식.**

### 버전 흐름

- SSL 2.0/3.0 → 폐기
- **TLS 1.2**: 현재 가장 널리 사용
- **TLS 1.3**: Handshake 단축, 더 강력한 보안

> “SSL 인증서”라고 부르지만 실제로는 TLS에서 사용하는 X.509 인증서를 의미.

---

# 9️⃣ XSS 공격

### ✔ XSS 개념

웹 페이지에 악성 JavaScript를 삽입하여 실행시키는 공격.

쿠키 탈취, 세션 하이재킹, 페이지 변조 등이 가능.

### ✔ 종류

- **Stored(저장형)**: 게시판 등에 저장되어 지속적으로 실행
- **Reflected(반사형)**: 요청 파라미터를 그대로 응답에 반영
- **DOM 기반**: 클라이언트 JS에서 발생

### ✔ 방어 방법

- 출력 인코딩(escape)
- CSP 적용
- HttpOnly 쿠키
- 위험한 API(`innerHTML`, `dangerouslySetInnerHTML`) 최소화

> “React 사용하면 XSS 안 생김”은 오해 — unsafe API 사용 시 여전히 취약.

---

# 🔟 CSRF vs XSS

| 항목      | XSS                      | CSRF                       |
| --------- | ------------------------ | -------------------------- |
| 공격 대상 | 사용자 브라우저          | 서버(인증된 사용자로 위장) |
| 발생 위치 | 피해 사이트              | 공격자 사이트              |
| 목적      | 스크립트 실행, 쿠키 탈취 | 서버에 악성 요청 전송      |
| 방어      | 인코딩, CSP              | CSRF 토큰, SameSite 쿠키   |

### 핵심 차이

**XSS = 스크립트를 주입해 실행**

**CSRF = 사용자를 속여 요청을 보내게 함**
